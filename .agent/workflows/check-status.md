---
description: Check project status (git, deps, running processes)
---

// turbo-all

1. Check git status:
```
git status
```

2. Check git log:
```
git log -n 5 --oneline
```

3. Check Python version:
```
python --version
```

4. Check installed packages:
```
.\venv\Scripts\pip list --format=columns
```
